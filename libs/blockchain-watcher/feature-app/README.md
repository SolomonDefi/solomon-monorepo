# blockchain-watcher-feature-app

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test blockchain-watcher-feature-app` to execute the unit tests via [Jest](https://jestjs.io).
