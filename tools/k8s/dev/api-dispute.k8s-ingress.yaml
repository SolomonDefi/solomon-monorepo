apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-api-dispute
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: 'false'
spec:
  rules:
    - host: api-dispute.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-dispute
                port:
                  number: 3000
